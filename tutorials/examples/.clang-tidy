---
# The unchanged root lint job discovers every tracked *.cpp. Its root CMake
# database intentionally excludes this independent target. Supply fallback flags
# for that root-directory invocation; inherit all existing diagnostic checks.
InheritParentConfig: true
ExtraArgsBefore: ['-std=c++20', '-Iinclude']
